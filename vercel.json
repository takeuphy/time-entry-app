{
  "buildCommand": "export DATABASE_URL=\"${DATABASE_URL:-$POSTGRES_URL}\" && prisma generate && prisma db push && next build",
  "crons": [
    {
      "path": "/api/send-daily-report",
      "schedule": "0 0 * * *"
    }
  ]
}
